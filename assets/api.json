{
  "openapi": "3.0.0",
  "paths": {
    "/v1/patients/sieve": {
      "post": {
        "operationId": "PatientsController_sieve",
        "summary": "Search/Filter all patients",
        "description": "Get a list of all patients filtered and/or searched",
        "parameters": [
          {
            "name": "skip",
            "required": false,
            "in": "query",
            "description": "Pagination: offset from where entities should start to be taken (how many to skip over). An integer greater than or equal to 0.",
            "schema": {
              "default": 0,
              "type": "number"
            }
          },
          {
            "name": "take",
            "required": false,
            "in": "query",
            "description": "Pagination: the number of entities that should be taken (the limit). An integer greater than 0.",
            "schema": {
              "default": 25,
              "type": "number"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {}
          }
        },
        "security": [
          {
            "Auth0": ["openid"]
          }
        ]
      }
    }
  },
  "components": {}
}
